import * as React from "react";
import {Fragment} from "react";

import "./css/follow_line_react_refactor.css";

const FollowLineReactRefactor = (props) => {
  return (
    <Fragment>
    {props.children}
    </Fragment>
  );
};

export default FollowLineReactRefactor;