import * as React from "react";
import {Fragment} from "react";

import "./css/LaserMapping.css";

const LaserMapping = (props) => {
  return (
    <Fragment>
    {props.children}
    </Fragment>
  );
};

export default LaserMapping;