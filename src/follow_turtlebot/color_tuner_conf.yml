ColorTuner:
  Camera:
    Server: 1 # 0 -> Deactivate, 1 -> Ice , 2 -> ROS
    Proxy: "Camera:tcp -h localhost -p 9000"
    Format: RGB8
    Topic: "/usb_cam/image_raw"
    Name: cameraA
    Fps: 30

NodeName: ColorTuner
